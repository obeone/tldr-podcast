[project]
name = "tldr-podcast"
version = "1.7.3"
description = "Converts TLDR newsletters to a two-voice podcast MP3 using Gemini"
readme = "README.md"
requires-python = ">=3.13"
authors = [
    { name = "Grégoire Compagnon", email = "obeone@obeone.org" },
]
license = "MIT"
license-files = ["LICENSE"]
dependencies = [
    "audioop-lts>=0.2.2",
    "beautifulsoup4>=4.14",
    "click>=8.3.3",
    "coloredlogs>=15.0.1",
    "google-genai>=2.2",
    "httpx>=0.28",
    "lxml>=6.1",
    "pydub>=0.25.1",
    "python-dotenv>=1.2.2",
    "pyyaml>=6.0.3",
    "questionary>=2.1",
    "rich>=15.0",
    "tenacity>=9.1",
    "trafilatura>=2.0.0",
]

[project.optional-dependencies]
cloak = ["cloakbrowser>=0.1"]

[dependency-groups]
dev = [
    "pytest>=9.0.3",
    "pytest-cov>=7.1",
]

[tool.pytest.ini_options]
testpaths = ["tests"]

[project.scripts]
tldr-podcast = "tldr.cli:cli"

[project.urls]
Homepage = "https://github.com/obeone/tldr-podcast"
Repository = "https://github.com/obeone/tldr-podcast"
Issues = "https://github.com/obeone/tldr-podcast/issues"

[build-system]
requires = ["setuptools>=77"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
where = ["src"]
